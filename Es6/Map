Map
键值对 键不限于字符串
.set() => 返回Map结构
.get() => value | undefined
.size() => 属性返回 Map 结构的成员总数
.has() => true|false
.delete => true|false
.clear => 没有返回值


.keys() => 返回键名的遍历器。
.values() => 返回键值的遍历器。
.entries() => 返回所有成员的遍历器。
.forEach() => 遍历 Map 的所有成员。

Map => 数组
[...map.keys()]
数组 => Map
new Map(array)
